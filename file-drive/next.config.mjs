/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: 'animated-mastiff-177.convex.cloud',
      },
    ],
  },
};

export default nextConfig;
